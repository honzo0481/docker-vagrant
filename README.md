# Docker/Trusty64 environment
vagrantfile and shell script for bootstrapping a docker environment.
usage:
```
>> git clone git@github.com:honzo0481/vagrant-docker.git
>> cd vagrant-docker
>> vagrant up
```
